// Write a "c" program defermine whether a person is cligible to vote::

#include<stdio.h>
int main()
{
    int age;
    printf("\n Enter The Age::");
    scanf("%d",&age);

    if(age>=18)

    printf("\n your are eligible to vote");

    return 0;
}