// Write a "c" program to print the ASCII value of a charater::

#include<stdio.h>
int main()
{
    char ch;
    printf("Printf The ASCII Value Of character::");
    scanf("%c",&ch);

    printf("ASSCII Value is %c is %d",ch,ch);

    return 0;
}